# API 레퍼런스

`lowlevel` 패키지의 모듈별 레퍼런스다 (docstring 자동 추출). 단위: 각도 rad, 속도 rad/s, 토크 N·m, 시간 s.
논리 관절 순서는 항상 `FL(abad, thigh, calf), FR, RL, RR` 이다.

## lowlevel.common — 관절 순서·매핑·한계·데이터 구조

```{eval-rst}
.. automodule:: lowlevel.common
```

## lowlevel.safety — 안전 가드

```{eval-rst}
.. automodule:: lowlevel.safety
```

## lowlevel.programs — 동작 프로그램

```{eval-rst}
.. automodule:: lowlevel.programs
```

## lowlevel.runtime — 실행 보조와 Session

```{eval-rst}
.. automodule:: lowlevel.runtime
```

## lowlevel.backend_mujoco — MuJoCo 백엔드

```{eval-rst}
.. automodule:: lowlevel.backend_mujoco
```

## lowlevel.backend_dds — 실기 DDS 백엔드

```{eval-rst}
.. automodule:: lowlevel.backend_dds
```

## lowlevel.virtual_plant — 시험용 가상 로봇 몸체

```{eval-rst}
.. automodule:: lowlevel.virtual_plant
```
